SERVICE_HOST=example.com        # 必須: 監視対象のホスト名

# Optional settings (fall back to defaults if unset)
# LOG_DIR=./log                  # default: script directory + /log
# LOG_FILE=./log/error.log       # default: LOG_DIR/error.log
# PING_BIN=/sbin/ping            # default: /sbin/ping
# NETWORK_CHECK_TARGET=8.8.8.8   # default: 8.8.8.8
# PING_COUNT=1                   # default: 1
# PING_TIMEOUT=1                 # default: 1 (second)
# RETRY_COUNT=3                  # default: 3
# RETRY_INTERVAL=5               # default: 5 (seconds)
